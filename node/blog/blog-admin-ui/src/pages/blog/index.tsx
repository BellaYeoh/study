import React from 'react';
import { BlogManagementProps } from './index.d';

const BlogManagement: React.FC<BlogManagementProps> = (props) => {
  return <div></div>;
};

export default BlogManagement;
